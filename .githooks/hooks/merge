#!/usr/bin/env bash
# gate-managed hook — delegates to the Canon-installed gate binary
if command -v gate >/dev/null 2>&1; then
  exec gate merge
fi
echo "gate binary not found on PATH; install it from Canon" >&2
exit 1
