[package]
name = "omenic-web-components"
edition = "2024"
version = "0.1.0"
publish = false

[dependencies]
omenic-web-state = { path = "../state" }
dioxus = { version = "0.7" }
pulldown-cmark = { workspace = true }
