[package]
name = "memory"
version = "0.1.0"
edition = "2024"
description = "Persistent local memory: append-only JSONL store, default-off"
license = "MIT"
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
