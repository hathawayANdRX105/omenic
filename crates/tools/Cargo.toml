[package]
name = "tools"
version = "0.1.0"
edition = "2024"
description = "Built-in agent tools: read_file / write_file / edit / run_bash"
license = "MIT"
publish = false

[dependencies]
llm = { path = "../llm" }
memory = { path = "../memory" }
serde_json = { workspace = true }
diffy = { workspace = true }
store = { path = "../store" }
protocol = { path = "../protocol" }
jobs = { path = "../jobs" }
terminal = { path = "../terminal" }
web-tool = { path = "../web-tool" }
serde = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
