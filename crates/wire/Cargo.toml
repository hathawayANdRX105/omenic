[package]
name = "wire"
version = "0.1.0"
edition = "2024"
description = "JSON-RPC 2.0 framing primitives for stdio transports"
license = "MIT"
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }