[package]
name = "skill"
version = "0.1.0"
edition = "2024"
description = "Project-local skill discovery, bounded catalog, and the skill meta-tool"
license = "MIT"
publish = false

[dependencies]
protocol = { path = "../protocol" }
plugin = { path = "../plugin" }
prompt = { path = "prompt" }
tools-harness = { path = "../tools/harness" }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
plugin = { path = "../plugin" }
tools-harness = { path = "../tools/harness" }
tempfile = { workspace = true }