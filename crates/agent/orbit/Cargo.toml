[package]
name = "orbit"
version = "0.1.0"
edition = "2024"
description = "Agent invariant loop: stream → tool_calls → execute → backfill → repeat"
license = "MIT"
publish = false

[dependencies]
llm = { path = "../../llm" }
tools = { path = "../../tools" }
serde = { workspace = true }
serde_json = { workspace = true }
fs2 = { workspace = true }
protocol = { workspace = true }
prompts = { path = "../../prompts" }
plugin = { path = "../../plugin" }
compaction = { path = "../../agent-loop/compaction" }
instruction = { path = "../../agent-loop/instruction" }

[dev-dependencies]
tempfile = { workspace = true }

