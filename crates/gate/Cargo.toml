[package]
name = "gate"
version = "0.1.0"
edition = "2024"
description = "GitHub artifact validation: rules, tools, shared API helpers"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
regex = "1"
anyhow = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
