[package]
name = "spec"
version = "0.1.0"
edition = "2024"
description = "Spec tables: markdown skeletons for GitHub artifacts"
license = "MIT"
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
