[package]
name = "guard"
version = "0.1.0"
edition = "2024"
description = "Loop-hygiene guards: repeat-tool-reminder and timeout-policy"
license = "MIT"
publish = false

[dependencies]
protocol = { path = "../protocol" }
plugin = { path = "../plugin" }
tools-harness = { path = "../tools/harness" }
parking_lot = "0.12"
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
