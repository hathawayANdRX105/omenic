//! CLI command layer.
//!
//! Subcommands: task add/done/status, plan, run, steer, abort.
//! Implemented in M1.8 (task) / M1.9 (plan) / M2 (run/steer/abort).

pub fn run() {
    // M1.8: parse args and route to task/plan/run/steer/abort.
}
